import React from "react";
import { createRoot } from "react-dom/client";
import PlanoRelocacao from "./PlanoRelocacao.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PlanoRelocacao />
  </React.StrictMode>
);
